# 작업 기록

> 설계 문서(`HANDOFF.md`)에서 출발해 API 검증 → 순수 레이어 → 데스크톱 앱까지 만든 과정.
> 판정의 근거 데이터와 표는 `FINDINGS.md`에 있다. 이 문서는 **무엇을 왜 그렇게 정했는지**를 남긴다.

---

## 1. 원칙 — 코드보다 검증이 먼저

`HANDOFF.md` §10에 미해결 항목이 8개 있었고, 그중 둘은 `[최우선]`으로 표시돼 있었다.
그래서 순서를 이렇게 잡았다.

1. API 클라이언트와 `/auctions/options` 덤프만 만들고 **멈춤**
2. 응답 구조를 눈으로 확인한 뒤 모델 설계
3. `probe.py`로 미해결 항목 판정하고 **멈춤**
4. 판정 결과를 반영해 나머지 구현

이 순서 덕에 얻은 것이 있다. 만약 코드부터 다 짰다면 `stat_ranges.json` 기반 품질 환산
레이어를 통째로 만들고 버렸을 것이고, 더 나쁘게는 `Sort` 값 오타 때문에 정렬이 안 먹는 줄
모른 채 "시장 하단 표본"이라 믿고 교환비를 추정했을 것이다.

**핵심 교훈: 이 API는 모르는 필드를 400 없이 조용히 버린다.**
그래서 필터가 안 먹어도 정상 응답이 온다. 모든 필터 판정에 대조군(`ZZZ_NoSuchField`)을 뒀다.

---

## 2. 실측으로 뒤집힌 가정

| # | 설계의 가정 | 실측 결과 |
|---|---|---|
| 1 | 품질 공식 = `GradeQuality`일 것 | **아니다.** 힘민지 단독으로는 단조성조차 없다 |
| 2 | 체력이 힘민지와 품질 롤을 공유할 수도 | **독립 롤.** 상관 −0.32 ~ +0.14 |
| 3 | 연마 단계 직접 필터가 없을 수도 | **있다.** `ItemUpgradeLevel` (정확일치) |
| 4 | `buy_price = 0` 매물이 앞을 막을 것 | **`0`이 아니라 `null`.** `BUY_PRICE ASC`에서 뒤로 밀린다 |
| 5 | 연마 옵션은 상/중/하 3등급 | API는 등급이 아니라 `EtcValues` 11~12단계 값 목록을 준다 |
| 6 | 팔찌도 같은 로직으로 | `GradeQuality`·`UpgradeLevel` 둘 다 `null`. 축이 다르다 |

### 설계에 없던 발견

- **`Sort` 값은 언더스코어 표기다.** `BUYPRICE`는 인식되지 않고 헛소리 값과 동일하게
  기본 정렬(StartPrice 오름차순)로 떨어진다. 그때는 `SortCondition`도 무시된다. 정답은 `BUY_PRICE`.
- **`MinValue`는 `MaxValue`가 `null`이면 쌍째로 버려진다.** 옵션 등급 조건이 100% 안 걸리고 있었다.
- **`EtcSubs[].Categorys`가 부위별 옵션 풀을 이미 준다.** 하드코딩할 필요가 없다.
- **아크 패시브(깨달음/도약)는 T4 전용 축**이지만 연마 단계에만 종속이라 비교 축이 아니다.

---

## 3. 품질 공식 — 최종

세션 중반에 "범위 테이블 폐기"로 결론 냈다가, 다시 대조해 **테이블이 유효함**을 확인했다.
반증된 것은 "그 값이 `GradeQuality`와 같다"는 등식뿐이었다.

```
값     = ceil(min[부위][단계] + width[부위] × 품질 / 100)      ← 게임이 올림
품질   = floor((값 − min[부위][단계]) / width[부위] × 100)

GradeQuality = floor((힘민지품질 + 체력품질 + 400) / 6)
```

- 힘민지·체력 **둘 다** 이 격자 위에 정확히 얹힌다 (격자 밖 0건)
- `GradeQuality` 재현 **960/960, 오차 0**
- 두 품질이 0이어도 `400/6 = 66` → **고대 T4는 `GradeQuality`가 66~100 구간만 쓴다**

`GradeQuality`는 35단계뿐이라 힘민지 101단계보다 훨씬 거칠다.
같은 `GradeQuality`에서 힘민지가 최대 2546 벌어진다.
**이것이 힘민지 품질을 따로 표시하는 이유다.**

`data/stat_ranges.json`에 부위·단계별 표가 있다.

---

## 4. 중간에 틀렸다가 바로잡은 것

기록해 두는 편이 낫다. 같은 함정을 다시 밟지 않으려면.

| 잘못 | 원인 | 바로잡은 방법 |
|---|---|---|
| "범위 테이블 폐기" 결론 | `GradeQuality`와 안 맞는다는 이유로 테이블 자체를 버림 | 격자 검사로 재대조 → 테이블 유효 |
| 격자 검사에서 "불일치" 판정 | `round` 가정 | 역산값이 항상 정수보다 근소하게 큼 → **`ceil`** |
| 가중 `w = 1/6` 추정 | 체력 폭을 최소자승으로 잘못 추정한 상태에서 도출 | 체력 격자를 먼저 확정 → 정확한 정수식이 나옴 |
| 체력 step 추정 실패 | "최소 간격 = step" 휴리스틱 | step이 2~4라 간격이 3/4로 섞여 나옴 → **파라미터 직접 탐색** |
| 정렬 프로브의 "무시됨" 오표기 | 기준선과 같으면 무조건 무시로 판정 | 전 범위 조건은 같은 게 정상 → 의도 열 추가 |

### 사용자 지적으로 갈린 것

- **체력 범위 `3323 / 2374 / 1900`** — 내 3연마 표(`3754 / 2682 / 2146`)와 충돌하는 줄 알았으나,
  직접 조회해 보니 3연마에서 내 하한 아래는 매물이 0건이었다.
  준 숫자는 **0연마 최소값**이었고 실측하니 정확히 일치했다. 둘 다 맞았고 가리키는 게 달랐다.
- **`Icon` 필드** — 정규화에서 버리고 있었다. 살려서 팔찌 아이콘을 해결했다.

---

## 5. 구조

```
loa/            순수 레이어 — PySide6 의존성 0 (검증됨)
  client.py       슬라이딩 윈도우 rate limiter(90/min), 429/5xx 재시도
  options.py      /auctions/options 캐시(TTL 24h) + 부위별 옵션 풀
  search.py       요청 바디 생성 + 페이지 수집 제너레이터
  models.py       Listing / UpgradeOption
  normalize.py    응답 → Listing. 구조 가정이 깨지면 예외
  quality.py      품질 환산 + 최소자승(stdlib 가우스 소거)
  export.py       CSV — CLI와 앱이 같은 함수를 쓴다

app/            UI 레이어
  main.py         진입점. 키 없으면 다이얼로그 먼저
  window.py       QMainWindow · 진행률 · 취소 · CSV
  search_panel.py 동적 검색 폼 + 연마 하한선 규칙
  table.py        모델 · 요약 계산 · 입찰 전용 필터
  card.py         카드 렌더러 (QListView 델리게이트)
  assets.py       아이콘 — 로컬 파일 → CDN 캐시
  worker.py       QThread 수집 워커

scripts/        검증 스크립트 (probe_*.py 는 전부 재현 가능)
data/           검증 산출물
```

의존 방향은 단방향이다. `loa/`는 `app/`을 모른다.

### `data/` 파일 3종의 구분

| 파일 | 성격 | 만든 것 |
|---|---|---|
| `stat_ranges.json` | **확정 환산표.** 격자 검증 통과 | `probe_quality_formula.py`, `probe_hp_quality.py` |
| `upgrade_grades.json` | 관측된 고대 등급값 [하,중,상] | `probe_grades.py` · 검색할 때마다 자동 누적 |
| `observed_ranges.json` | 표본에서 본 범위일 뿐 | `probe.py` |

앞의 둘을 섞지 않으려고 파일을 나눴다. 처음엔 한 파일이었고 `probe.py`가 확정표를 관측치로
덮어쓰고 있었다.

---

## 6. 연마 옵션 등급을 만든 방법

`EtcValues`는 아이템 등급 4단계 × 하/중/상 12개가 섞여 있고,
**고대 3개의 인덱스가 옵션마다 다르다.**

```
적주피   [4, 9, 11]      공퍼    [3, 9, 11]
공격력+  [8, 10, 11]     최대마나 [2, 6, 9]
```

인덱스 규칙이 안 선다. 그래서 `ItemGrade='고대'` 고정 시 옵션당 값이 3개로 좁혀지는 성질을 쓴다 —
**실제 매물에서 관측된 distinct 값을 정렬해 순위를 매긴다.**

저가 매물만 보면 상급이 안 잡히므로 `BUY_PRICE` **ASC와 DESC 양끝**을 훑었다.
540건으로 18개 옵션 전부 하/중/상을 확보했다.

---

## 7. UI 변천

| 단계 | 내용 |
|---|---|
| 1 | 테이블 + Δ를 같은 셀에 겹쳐 쓰는 델리게이트 |
| 2 | 등급 색 (하 하늘 · 중 보라 · 상 금색), 연마 조합 3줄 고정 |
| 3 | 특수 옵션 우선 정렬 → 등급 상→중→하 우선으로 변경 |
| 4 | `기준 대비` 요약 셀 — 일치하는 항목은 줄 자체가 안 생긴다 |
| 5 | 카드 뷰로 전환 (`QListView` + 델리게이트, 가상화 유지) |
| 6 | 아이콘·품질 배지·최소 입찰가, 여백 압축 → 비교 열 확대 |

### 표시상의 결정

- **힘민지가 주축, `GradeQuality`는 보조.** 같은 `GradeQuality`에서 힘민지가 2546 벌어지므로.
- **좋으면 녹색, 나쁘면 빨강.** 스탯은 오르면 녹색, 가격은 오르면 빨강. 방향은 반대지만 의미는 같다.
- **품질 색은 89 이하 파랑 / 90 이상 보라.**
- **`입찰가 0`은 "아직 아무도 입찰 안 함"**이다. 480건을 훑어 입찰이 붙은 17건이 전부
  `BuyPrice = None`임을 확인했다. 즉구가가 있으면 그냥 즉구로 사가서 입찰이 안 붙는다.
  그래서 `0G` 대신 **최소 입찰가**(`BidStartPrice`, 현재가 ×1.05)를 보여준다.
- **팔찌는 열 구성 자체가 다르다.** 연마 라디오가 사라지고 품질 필터가 꺼진다.

---

## 8. 실행

```bash
python -m venv .venv
.venv/Scripts/python.exe -m pip install -r requirements.txt

.venv/Scripts/python.exe scripts/setup_key.py     # API 키 입력 (.env 저장)
.venv/Scripts/python.exe -m app                   # 앱 실행
```

키는 `.env` 평문이다(`keyring` 전환은 보류). `.gitignore`에 있어 커밋되지 않는다.

검증 재현:

```bash
scripts/probe.py                  # §10 항목 전반 → FINDINGS.md
scripts/probe_quality_formula.py  # 힘민지 격자 (API 호출 없음)
scripts/probe_hp_quality.py       # 체력 격자 + GradeQuality 재현
scripts/probe_grades.py           # 연마 옵션 하/중/상
scripts/probe_minvalue.py         # MinValue/MaxValue 동작
scripts/probe_sort.py             # Sort 값 동작
```

CSV 반출:

```bash
scripts/dump_csv.py --category 200010 --upgrade 2 --pages 5 [--with-hp]
```

---

## 9. 남은 것

**비교 엔진(§6)이 없다.** 지금 앱은 §6.5의 **1층(관측된 사실)만** 보여준다.
기준 대비 Δ는 뺄셈이라 틀릴 수 없지만, `공퍼 중→상 = +4,200골드` 같은 **2층 추정은 미착수**다.
쌍 탐색, 교환비 추정, 다중공선성 판정, 근거 쌍 목록 펼치기가 여기에 들어간다.

그 외:

- 프리셋 / `keyring` / 세션 복원 / PyInstaller 패키징 (§8.4)
- `README.md` 없음
- 팔찌 코호트 기준을 따로 설계해야 함 (품질도 연마도 없으므로)
- 같은 부위에 `도래한 결전`과 `마주한 종언` 두 이름이 섞인다. 스탯 범위가 같은지 미검증
- `IsCompetitive`가 480건 전부 `false`였다. 무엇을 뜻하는지 모른다
- 연마 옵션 상급 일부는 여전히 미관측 (저가 표본 편중)
