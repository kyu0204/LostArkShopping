# 로스트아크 경매장 매물 비교기 — 설계 문서

> 상태: 설계 확정, 구현 미착수
> 형태: PySide6 데스크톱 애플리케이션 (오버레이 아님, 켜고 끄는 일반 창)

---

## 1. 이 도구가 하는 일

같은 조건으로 검색된 경매장 매물들 사이에서 **스펙 차이와 가격 차이가 각각 얼마인지** 보여준다.

**현 단계에서 이 도구는 가치 판단을 하지 않는다.** "이게 이득이다", "저평가다" 같은 판정을 내리지 않는다. 차이를 정확히 계산해서 제시하고, 어느 쪽이 나은지는 사용자가 정한다.

> 추후 명확한 비교 기준(딜 환산 계수 등)이 확립되면 가치 판단 레이어를 **추가**할 예정. 단, 1층(관측된 사실)과 분리된 별도 레이어로 얹으며 기존 출력을 대체하지 않는다. §5.5 참조.

### 범위에 포함
- 장신구(목걸이/귀걸이/반지), 팔찌
- 검색 시에만 API 조회 (백그라운드 수집 없음, DB 없음)
- 코호트 내 상대 비교 및 옵션별 암묵 교환비 추정

### 범위에서 제외
- 시세 히스토리 축적, 시계열 차트
- 저평가 매물 알림 / 스나이핑
- 세트 조합 최적화 (목/귀/귀/반/반 5부위 조합)
- 딜 계산기 연동, 골드당 딜증% 산출
- 회귀 기반 가격 예측 모델

---

## 2. 데이터 소스와 제약

### 엔드포인트
- `POST /auctions/items` — 경매장 매물 검색
- `GET /auctions/options` — 카테고리/등급/EtcOptions 목록 및 값 범위
- Base: `https://developer-lostark.game.onstove.com`
- 인증: `Authorization: bearer {JWT}`

### 제약
| 항목 | 값 | 영향 |
|---|---|---|
| 페이지당 건수 | 10 | 전량 수집이 비현실적 |
| Rate limit | 100 req/min (키당) | 검색 1회 예산 관리 필요 |
| 최근 거래(시세) | **API 미제공** | 절대 시세 기준선 없음 → 코호트 내 상대 비교만 가능 |

### 옵션 목록은 하드코딩 금지
`GET /auctions/options` 응답으로 검색 폼을 **동적 생성**한다. 부위(카테고리 코드)별로 붙을 수 있는 옵션 풀과 값 범위가 다르므로, 부위 선택 시 해당 옵션만 노출한다.

> 티어3/4 장신구 통합 패치 당시 검색 조건이 바뀌면서 옵션을 하드코딩한 서드파티 앱들이 동작 불능이 된 전례가 있다. 옵션 스키마는 반드시 런타임에 받아온다.

캐시: 응답을 로컬에 저장, TTL 24시간.

---

## 3. 검색 조건

### 3.1 고정값 (사용자 선택 불가)

```
ItemGrade = "고대"
ItemTier  = 4
```

폼에서 제외하고 헤더 배지로만 표시한다. 부수 효과로 코호트가 균질해져 비교 품질이 올라간다.

### 3.2 연마 단계 — 단일 선택 + 하한선

사용자가 **1~3연마 중 하나를 선택**한다. 복수 선택 없음.

**하한선 규칙: 지정한 연마 옵션 개수 = 최소 연마 단계.**

| 지정한 옵션 수 | 선택 가능한 연마 단계 |
|---|---|
| 0 | 1, 2, 3 |
| 1 | 1, 2, 3 |
| 2 | 2, 3 |
| 3 | 3 |

UI 동작:
- 옵션을 추가하면 하위 단계 라디오가 즉시 비활성화된다
- 이미 하한선 미만이 선택돼 있었다면 하한선으로 자동 상향
- 옵션을 제거하면 다시 활성화

**설계적 귀결:** 단계를 사전에 확정하므로 §5.1의 사후 코호트 분할과 단계별 섹션 분리가 **불필요해진다.** 요청 단계에서 단일 단계만 가져오면 화면이 단순해지고 페이지 예산도 절약된다.

> 확인 필요: API에 연마 단계 직접 필터가 있는지. 없다면 `EtcOptions`의 옵션 수량 조건으로 대체 가능한지 검증. 둘 다 불가하면 응답을 받아 클라이언트에서 필터링(페이지 예산 손실 발생).

### 3.3 연마 옵션 선택

`/auctions/options`에서 받은 부위별 옵션 풀로 동적 폼 생성. 사용자가 옵션 + 등급(상/중/하)을 지정하면 `EtcOptions` 배열에 실어 전송.

---

## 4. 데이터 모델

### 4.1 정규화된 매물

```python
@dataclass
class Listing:
    raw_index: int              # 원본 응답 내 위치 (디버깅용)

    # 가격
    buy_price: int | None       # 즉시구매가. None이면 입찰 전용
    bid_price: int
    trade_allow_count: int      # 거래 가능 횟수 — 비교 축에 포함할 것
    end_date: str

    # 기본 스탯
    stat_raw: dict[str, int]      # {"힘민지": 11040, "체력": 620}  ← 계산의 원천
    stat_quality: dict[str, float]  # {"힘민지": 97.1, "체력": ...}  ← 표시용
    api_quality: int | None       # GradeQuality (검증용, §4.3)

    # 연마
    polish_level: int           # 1~3
    discrete: dict[str, str]    # {옵션ID: "상"|"중"|"하"}  ← 없는 옵션은 키 자체가 없음
    continuous: dict[str, float]  # {옵션ID: 수치}
```

### 4.2 이산 / 연속 구분

| 구분 | 해당 항목 | Δ 표현 |
|---|---|---|
| **연속** | 기본 힘민지·체력, 연마 옵션의 체력·공격력·무기공격력 | 뺄셈 (`+18`) |
| **이산** | 적주피·추피 등 상/중/하 등급 옵션 | 전이 (`없음 → 중`) |

**주의사항**

- 공격력과 무기공격력은 **독립 변수**로 둔다. 곱연산 계열인 무공은 가격 영향이 크게 다르므로 합산 금지.
- 같은 "공격력"이라도 `IsValuePercentage` 플래그에 따라 %형과 실수치형이 별개 옵션이다. 키를 분리할 것.
- 이산 옵션은 **순서 없는 집합**이다. API 응답의 `Options` 배열 순서를 신뢰하면 안 된다. 옵션ID를 키로 하는 맵으로 정규화해야 `적주피+추피`와 `추피+적주피`를 동일 사양으로 인식한다.

### 4.3 기본 스탯 → 0~100 품질 환산

힘민지와 체력은 자릿수가 달라(11,040 vs 620) 눈으로 비교가 안 된다. 동일 척도로 정규화해서 표시한다.

```
품질 = (실수치 - 최소[부위][단계]) / 범위폭[부위] × 100
```

**계산은 실수치로, 표시만 품질로.** 품질 1이 힘민지 19~27에 해당하므로 품질로 반올림하면 그보다 작은 차이가 뭉개진다. `stat_raw`가 항상 진실의 원천이다.

#### 힘민지 범위 테이블 (고대 / 티어4)

범위 폭이 연마 단계와 무관하게 부위별로 고정이다. 단계는 창을 통째로 이동시킬 뿐.

`data/stat_ranges.json` — **코드가 아닌 별도 파일로 관리** (패치 시 파일만 교체)

```json
{
  "ancient_t4": {
    "힘민지": {
      "반지":   { "width": 1935, "min_by_polish": [9156, 9414, 9930, 10962] },
      "귀걸이": { "width": 2083, "min_by_polish": [9861, 10139, 10695, 11806] },
      "목걸이": { "width": 2679, "min_by_polish": [12678, 13035, 13749, 15178] }
    }
  }
}
```

`min_by_polish` 인덱스 = 연마 단계(0~3). 최대값 = `min + width`.

| 부위 | 단계 | 최소 | 최대 |
|---|---|---|---|
| 반지 | 0 / 1 / 2 / 3 | 9156 / 9414 / 9930 / 10962 | 11091 / 11349 / 11865 / 12897 |
| 귀걸이 | 0 / 1 / 2 / 3 | 9861 / 10139 / 10695 / 11806 | 11944 / 12222 / 12778 / 13889 |
| 목걸이 | 0 / 1 / 2 / 3 | 12678 / 13035 / 13749 / 15178 | 15357 / 15714 / 16428 / 17857 |

#### ⚠ 최우선 검증 항목

이 공식은 **게임 내 품질 수치의 정의 그 자체일 가능성이 높다.** (반지 0연마: `9156 + 1935 × 100/100 = 11091` — 최대값과 정확히 일치)

구현 초기에 실제 API 응답으로 다음을 대조할 것:

1. **계산된 품질 == `GradeQuality`인가?**
   - 일치 → 테이블 불필요. `GradeQuality`를 그대로 사용. 코드가 크게 단순해짐
   - 불일치 → 계산식이 정답. 테이블 유지
2. **체력이 힘민지와 같은 품질을 공유하는가?**
   - 공유 → 체력 범위 테이블 불필요 (품질 롤이 하나)
   - 독립 롤 → 체력 전용 범위 테이블 필요 (**현재 수치 미확보**)

### 4.4 부위 무지성 (part-agnostic) 원칙

비교 엔진은 옵션 키가 무엇인지 몰라야 한다. `/auctions/options`에서 받은 옵션 ID를 그대로 키로 사용하면 목걸이·귀걸이·반지·팔찌를 **단일 제네릭 로직**으로 처리할 수 있다. 부위를 아는 계층은 검색 폼 생성과 품질 환산까지만.

---

## 5. 수집 전략

### 5.1 기본 흐름

1. 1페이지 요청 → `TotalCount` 확인
2. 예상 페이지 수가 **15페이지 초과**면 사용자에게 확인
   - `조건에 맞는 매물 2,847건 — 전부 가져오려면 약 3분 소요. 상위 80건만 볼까요?`
   - 조건을 좁히도록 유도하는 것이 실제로 더 나은 결과를 준다 (§7.1 참조)
3. 임계치 이하면 바로 진행
4. **유효 매물 50~80건** 확보 후 쌍 충분성 검사
5. 충분하면 종료, 부족하면 최대 페이지까지 계속

### 5.2 종료 조건

```
중단 = (모든 주요 옵션의 쌍 ≥ 5) OR (최대 페이지 도달) OR (사용자 취소)
```

- **"유효 매물"** = `buy_price`가 존재하는 매물. 입찰 전용은 카운트에서 제외.
  → 페이지 수가 아니라 유효 건수 기준으로 세야 한다. `BUYPRICE ASC` 정렬 시 즉구가 없는 매물이 앞쪽에 몰리면 5페이지를 긁고도 유효 매물이 20건뿐일 수 있다.
- **"주요 옵션"** = 수집된 매물의 **20% 이상**에서 등장하는 옵션.
  → 이 필터가 없으면 1회만 등장한 희귀 옵션 때문에 영원히 종료되지 않는다. 20% 미만 옵션은 처음부터 `표본 부족`으로 확정.
- **쌍 충분성 검사는 20건 단위 배치로.** 매 페이지마다 전체 쌍 탐색을 재실행하면 낭비이고 조기 종료 판정도 불안정해진다.

### 5.3 구현 시 확인 필요

`BUYPRICE ASC` 정렬에서 `buy_price = 0`인 매물이 앞으로 오는지 뒤로 가는지 실제 응답으로 확인할 것. 앞으로 온다면 실제 저가 매물에 도달하기 전에 페이지 예산을 소진하므로 정렬 전략 재검토가 필요하다.

---

## 6. 비교 엔진

### 6.1 코호트

검색 결과 = 코호트. **연마 단계가 검색 조건으로 확정되므로 코호트는 단일 단계로 균질하다.** 단계가 다른 매물끼리는 어떤 경우에도 비교하지 않는다.

> 단계 차이는 연마 재화와 시행 비용이 가격에 반영된 것이라 순수한 옵션 가치 차이가 아니다. 이는 절대 규칙이다.

### 6.2 기준점(baseline)

- 기본값: 코호트 내 최저 즉구가 매물
- **사용자가 임의 행을 클릭하면 그 매물이 기준이 되고 전체 Δ 재계산** ← 이 화면의 핵심 인터랙션
- 확장: 사용자가 보유 중인 악세를 가상 기준점으로 입력 → "업그레이드 비용" 조회

### 6.3 쌍 판정 규칙

두 매물이 **통제된 쌍(controlled pair)** 인 조건:

```
1. 이산 옵션 맵에서 정확히 하나의 키만 값이 다르다
   (키 부재 → 값 존재도 "다름"으로 취급: 없음 → 중)
2. 모든 연속값 차이가 임계치 이내
3. 양쪽 모두 buy_price가 존재한다
```

(연마 단계는 §6.1에 의해 이미 동일하므로 조건에서 생략)

임계치는 사용자 조절 가능하게 노출한다. 빡빡하면 쌍이 안 나오고 느슨하면 오염된다.

### 6.4 이중 추정 구조

연속값은 값이 제각각이라 엄격히 맞추면 쌍이 하나도 안 남는다. 따라서:

- **연속값 기울기**: 이산 옵션 조합이 동일한 그룹 내에서 회귀 → `품질 1당 340골드`
- **이산값 교환비**: 연속값이 근사한 매물끼리 쌍 매칭 → `공퍼 중 → 상 = +4,200골드`

### 6.5 출력은 2층 구조

**1층 — 관측된 사실 (추정 없음)**

기준 대비 (Δ가격, Δ옵션). API가 준 숫자의 뺄셈이므로 틀릴 수 없다.

```
+3,200골드 · 3번째 연마 없음→공퍼 중 · 힘민지 품질 +6.2
```

**2층 — 추정된 교환비 (근거 필수)**

```
공퍼 중 → 상     +4,200골드   (7쌍, 3,600~4,800)
추피 없음 → 하   +2,500골드   (3쌍, 2,100~2,900)
힘민지 품질 1당     340골드   (기울기, R²=0.71)
치적 없음 → 중    표본 부족   (1쌍)
```

- 쌍 개수와 범위를 **반드시 함께 표시**한다. 숫자만 주면 사용자가 확정값으로 받아들인다.
- 쌍이 2개인데 4,000과 12,000이면 중앙값 8,000을 내놓는 것은 거짓말이다. → `표본 부족`
- **각 항목 클릭 시 그 값이 도출된 실제 쌍 목록을 펼칠 수 있어야 한다.** 가치 판단을 사용자에게 넘긴다는 것은 계산 과정을 감사할 수 있다는 뜻이다.

**3층 (향후) — 가치 판단**

명확한 비교 기준이 확립되면 여기에 얹는다. 1·2층 출력을 대체하지 않고 추가 정렬 축으로만 동작해야 한다.

### 6.6 추정 불가 케이스

정직하게 `표본 부족` / `분리 불가`로 표시하고 환산에서 제외한다. 억지 추정값을 내놓지 않는다.

- 통제된 쌍 부족
- **다중공선성**: 두 옵션이 항상 함께 등장 (예: 공퍼 중인 매물이 전부 고품질)
- 팔찌는 옵션 조합이 희소해서 이 케이스가 자주 발생한다. 정상이다.

### 6.7 용어 주의

`BUYPRICE ASC` 정렬로 앞쪽만 수집하므로 표본은 **시장 하단**에 편중된다. 여기서 나온 4,200골드는 "공퍼 중의 시세"가 아니라 **"하한선 기준 상대가"** 다. UI 문구에서 반드시 구분할 것.

---

## 7. 화면 구조

### 7.1 열 구성은 검색 조건에서 파생된다

**원칙: 고정된 것은 열에서 빼고, 변하는 것만 열로 만든다.**

"적주피 상"으로 검색했으면 전원이 적주피 상이다. 그 열은 정보량이 0이므로 헤더 배지로만 표시하고 테이블에서 제거한다.

```
[고대 · T4 · 목걸이 · 2연마]  [고정: 적주피 상, 추피 중]  [비교 축: 품질, 거래횟수]
```

부수 효과: 검색을 조일수록 테이블이 좁아지고 읽기 쉬워진다. 사용자에게 "조건을 더 거는 게 낫다"는 신호가 자연스럽게 전달된다.

### 7.2 연마 옵션 표시

개별 열로 쪼개지 말고 **단일 "연마 조합" 열에 칩(chip)으로 나열**한다.

이 도구의 비교 단위는 개별 옵션이 아니라 조합이다. 사용자의 실제 판단은 "적주피상+추피중 조합이 저 조합보다 얼마 비싸냐"이지 "추피 열로 정렬해보자"가 아니다.

### 7.3 Δ 표시

절대값과 Δ를 **같은 셀에 겹쳐 쓴다.** 열을 두 배로 늘리지 않으면서 두 정보를 모두 전달한다.

```
┌──────────┐   ┌──────────┐
│  16,000  │   │   97.1   │  ← 품질 (표시)
│  +4,000  │   │   +6.2   │  ← 기준 대비 Δ
└──────────┘   └──────────┘
```

기본 스탯 셀은 hover 시 실수치(`11,040`)를 툴팁으로 노출한다.

기준 행은 sticky 또는 강조 배경으로 최상단 고정. 클릭으로 기준 전환 시 Δ 값만 갱신.

### 7.4 입찰 전용 매물 (즉구가 없음)

- **회색 처리로 표시** — 시장에 무엇이 있는지는 보여준다
- 스펙 열은 정상 표시, **가격 열은 비움** (`입찰 12,000 · 즉구 없음`)
- **쌍 탐색 및 모든 계산에서 자동 제외** (낙찰가를 알 수 없으므로 입찰가로 대체 불가)
- 필터 토글로 숨김 가능. **필터 기본값 = 켬(숨김)**
- 필터는 표시 여부만 바꾼다. 계산 참여 여부를 바꾸는 스위치가 아니다.
- 숨김 시에도 `입찰 전용 14건 숨김` 표시를 남긴다 (완전히 사라지면 시장 전체를 봤다고 착각)
- 회색 행 툴팁: `즉시구매가 없어 비교 불가`

### 7.5 정렬

단일 정답을 정하지 않는다. 여러 축을 열어둔다.

- 가격순 / 기준 대비 Δ가격순 / 품질순 / 특정 옵션 보유 여부

향후 가치 판단 레이어가 붙으면 "내 가중치로 정렬"을 추가.

### 7.6 내보내기

**CSV 내보내기는 1단계 필수 기능.** 추후 자체 계산 기준을 적용하려면 사용자가 원본 데이터를 자기 스프레드시트로 가져갈 수 있어야 한다. 실수치와 품질을 모두 열로 포함할 것.

---

## 8. 애플리케이션 구조

### 8.1 스택

- **PySide6** — `QMainWindow` 기반 일반 창
- 오버레이 아님: `WindowStaysOnTopHint`, 클릭 투과, 프레임리스, 게임 창 추적, 전역 핫키 **모두 불필요**
- 트레이 상주 안 함. 창 닫으면 프로세스 종료.
- 배포: PyInstaller 단일 exe

### 8.2 레이어

```
┌─────────────────────────────────────┐
│  UI (QMainWindow)                   │
│   검색 폼 / 결과 테이블 / 교환비 패널  │
├─────────────────────────────────────┤
│  비교 엔진  (순수 함수, UI 무관)      │
│   쌍 탐색 / 교환비 추정 / 품질 환산    │
├─────────────────────────────────────┤
│  API 클라이언트 (QThread 워커)        │
└─────────────────────────────────────┘
```

**비교 엔진은 순수 함수로 유지한다.** UI 프레임워크 의존성을 넣지 않으면 나중에 웹으로 옮기거나 CLI를 붙일 때 그대로 재사용된다.

### 8.3 스레딩

API 호출은 반드시 `QThread` 워커에서. 최대 15페이지까지 갈 수 있으므로 메인 스레드에서 돌리면 UI가 얼어붙는다.

- 페이지 단위 진행률 시그널: `3/15 페이지 · 유효 매물 24건`
- **취소 버튼 필수.** 사용자가 조건을 잘못 걸었다는 것을 수집 도중에 깨닫는 경우가 많다.

### 8.4 로컬 상태

| 항목 | 저장 방식 | 비고 |
|---|---|---|
| API 키 | `keyring` (OS 자격 증명 저장소) | JWT이므로 유출 시 타인이 한도를 소진. 평문 파일 금지 |
| 검색 프리셋 | JSON 파일 | 웹과 달리 URL 공유가 불가하므로 사실상 필수 기능 |
| 마지막 검색 조건 | JSON 파일 | 실행 시 자동 복원 — 켜자마자 검색 버튼만 누르면 되게 |
| `/auctions/options` | JSON 캐시, TTL 24h | |
| 스탯 범위 테이블 | `data/stat_ranges.json` | 코드에 박지 말 것 |

### 8.5 단축키

- `Enter` — 검색
- `Esc` — 수집 취소

---

## 9. 구현 순서 제안

1. API 클라이언트 + `/auctions/options` 파싱 → 부위별 옵션 풀 확보
2. 정규화 레이어 (`Listing` 변환) + CSV 내보내기
   → **여기서 §4.3 검증을 반드시 수행** (품질 공식 == GradeQuality? 체력은 독립 롤?)
   → §5.3 정렬 동작 확인도 이 시점에
3. 검색 폼 (부위 선택 → 동적 옵션 폼 → 연마 하한선 연동)
4. QThread 수집 워커 + 진행률/취소
5. 결과 테이블 (Δ 표시, 기준 행 전환)
6. 비교 엔진 (쌍 탐색 → 교환비 추정) + 교환비 패널
7. 프리셋 / 키 저장 / 세션 복원

2번이 끝나면 실데이터로 4.3의 두 질문이 해소되고, 그 결과에 따라 3번 이후 설계가 확정된다.

---

## 10. 미해결 사항

- [ ] **[최우선]** 계산된 품질 == `GradeQuality` 여부 (§4.3)
- [ ] **[최우선]** 체력이 힘민지와 품질 롤을 공유하는지 / 독립이면 체력 범위 테이블 확보
- [ ] API에 연마 단계 직접 필터가 있는지 (§3.2)
- [ ] `BUYPRICE ASC`에서 `buy_price = 0` 매물의 정렬 위치 (§5.3)
- [ ] 팔찌의 기본효과/특수효과 축 구분 — `/auctions/options` 실제 응답 확인 후 확정
- [ ] 팔찌에 §4.3 품질 환산이 적용 가능한지 (기본 스탯 구조가 다를 수 있음)
- [ ] 연속값 쌍 매칭 임계치의 적절한 기본값 (실데이터로 튜닝 필요)
- [ ] 연마 시스템에 2026년 5월 이후 변경이 있었는지 확인 (슬롯 수, 등급 체계)
